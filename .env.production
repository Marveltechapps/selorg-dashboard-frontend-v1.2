# Production build - used when running: npm run build
VITE_ENV=production
# Same-origin API avoids CORS; dashboard nginx must proxy /api/ to the backend.
# See deploy/nginx-dashboard.selorg.com.conf.example
VITE_API_BASE_URL=
VITE_WS_URL=
